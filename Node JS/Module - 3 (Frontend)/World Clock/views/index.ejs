<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Country Time</title>
    <link rel="stylesheet" href="css/style.css">
    <script src='https://cdn.tailwindcss.com'></script>
</head>

<body>
    <div class="main bg-zinc-800 min-h-screen flex flex-col justify-center items-center gap-3">
        <h1 class="text-4xl text-white font-semibold">World Clock</h1>
        <form action="/" method="POST" class="flex justify-center items-center gap-3 p-5 rounded-lg">
            <input type="text" id="country" name="country" placeholder="Enter Country Name"
                class="px-5 py-3 rounded-lg bg-zinc-600 text-zinc-200">
            <button type="submit" class="bg-blue-500 rounded-lg py-3 px-3 text-white font-semibold">Get Time</button>
        </form>

        <% if (country || time) { %>
            <div
                class="time-detail p-6 rounded-lg bg-zinc-600 text-white flex flex-col justify-center items-center gap-3">

                <span class="text-2xl text-zinc-400">
                    <%= country %>
                </span>
                <span class="text-4xl">
                    <%= time %>
                </span>
                <% } %>

                    <% if (error) { %>
                        <p style="color: red;">
                            <%= error %>
                        </p>
                        <% } %>
            </div>
    </div>
</body>

</html>